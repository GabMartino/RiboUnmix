"""Shared, read-only helpers for the numbered synthetic analyses.

The analysis entry points are intentionally kept separate from the training
pipeline.  This package only discovers files and computes summaries from
already written result bundles.
"""
